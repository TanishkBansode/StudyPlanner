<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidWebViewOfflineApplication"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="23"
        android:targetSdkVersion="34" />

    <!-- =================== ADDITION 1: PERMISSIONS =================== -->
    <!-- Allows the app to schedule precise alarms that fire at an exact time. This is critical for our alarm functionality. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Required on Android 13 (API 33) and higher to show any kind of notification. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <permission
        android:name="com.example.androidWebViewOfflineApplication.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.androidWebViewOfflineApplication.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" /> <!-- =============================================================== -->
    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:debuggable="true"
        android:extractNativeLibs="false"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.androidWebViewOfflineApplication" >
        <activity
            android:name="com.example.androidWebViewOfflineApplication.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting" > <!-- CHANGE THIS LINE -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============ ADDITION 2: RECEIVER DECLARATION ============= -->
        <!--
             This registers the AlarmReceiver with the Android system.
             - enabled="true": The receiver is active.
             - exported="false": The receiver cannot be triggered by other apps, only by our own app's AlarmManager. This is a security best practice.
        -->
        <receiver
            android:name="com.example.androidWebViewOfflineApplication.AlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- ... inside your <application> tag ... -->

        <activity
            android:name="com.example.androidWebViewOfflineApplication.AlarmActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:showOnLockScreen="true"
            android:turnScreenOn="true" />

        <receiver
            android:name="com.example.androidWebViewOfflineApplication.AlarmReceiver"
            android:enabled="true"
            android:exported="false" />
        <!-- =============================================================== -->

        <activity
            android:name="androidx.compose.ui.tooling.PreviewActivity"
            android:exported="true" />
        <activity
            android:name="androidx.activity.ComponentActivity"
            android:exported="true" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.example.androidWebViewOfflineApplication.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>